import React from "react";
// reuse the MoodTracker screen you already have inside the tabs folder
import MoodTracker from "./(tabs)/mood";

export default function MoodHistoryRoute() {
  return <MoodTracker />;
}